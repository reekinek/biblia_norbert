pakiet: isc-dhcp-server

## Instalacja (nie ważne na egzaminie)
Bierzemy internet z puszki, i instalujemy powyższy pakiet

# info

nie chce mi sie bo to proste

glowny config to ``/etc/dhcp/dhcpd.conf`` i tam doslownie jest wszystko co potrzeba ale zakomentowane wiec wystarczy odkomentowac i dostosowac

jest jeszcze `/etc/default/isc-dhcp-server` gdzie trzeba (albo nie?) dodac nazwe interfejsu na ktormy ma dhcp dzialac

komenda do wyswietlenia dzierzawionych adresow to `dhcp-lease-list`